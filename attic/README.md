# Attic — deprecated skills

Retired skills kept for reference. **Not shipped.**

## Rules
- A skill here must **not** appear in `.claude-plugin/marketplace.json` and must **not** live under `plugins/`.
- Prefer moving (not deleting) a retired skill here, with a one-line note at the top of its `SKILL.md` explaining why it was retired and what replaces it.
- `scripts/check-consistency.py` treats anything outside `plugins/` as not shipped.

(Empty for now — convention placeholder.)
